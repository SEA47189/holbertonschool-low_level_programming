#include "holberton.h"
#include <string.h>
/**
 *_strcat -combines two strings to create a new string
 *
 *@dest: destination string
 *
 *@src: source string
 *
 *Return: 0 = Success
 */
char *_strcat(char *dest, char *src)

{
char d;
char s;

for (; d != '\0'; d++)
for (; s != '\0'; s++)
{
while (d != '\0')
{
return (dest);
}
while (s != '\0')
{
return (src);
}
}
return (0);
}
