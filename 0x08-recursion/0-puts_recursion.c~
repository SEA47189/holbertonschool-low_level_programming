#include "main.h"
#include <string.h>
/**
 *_puts_recursion - prints string
 *
 *function - to print a string followed
 * by a new line
 *
 *@s: character
 *
 *Return: 0 = Success
 */
void _puts_recursion(char *s)
{
if (*s != '\0')
s++;
{
_putchar(*s);
_putchar('\n');
}
{
return;
}
